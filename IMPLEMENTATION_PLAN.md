## 阶段 1: 元数据扫描和保存接口
**目标**: 扫描 Album、Artist、Title 任一为空的音频，并提供安全的单条保存接口。
**成功标准**: 只允许修改扫描到的音频文件；保存前重新确认文件存在，保留原始报告和并发锁语义。
**测试**: 空字段识别、音频标签写入、非法路径和并发保存测试。
**状态**: 已完成

## 阶段 2: Web 元数据编辑界面
**目标**: 在现有控制台中展示问题歌曲，支持编辑 Album、Artist、Title 并保存。
**成功标准**: 页面能刷新问题列表，保存后移除已修复项目并显示成功/失败状态；不影响原有任务按钮。
**测试**: HTTP 列表、保存接口、令牌校验和前端脚本语法测试。
**状态**: 已完成

## 阶段 3: 回归验证和发布
**目标**: 完成文档、Compose 兼容性和镜像发布验证。
**成功标准**: 既有功能测试与新增测试通过，Compose 可解析，GitHub Actions 成功构建多架构镜像。
**测试**: 单元测试、编译检查、前端语法检查、Compose 配置检查和 GitHub Actions。
**状态**: 进行中
