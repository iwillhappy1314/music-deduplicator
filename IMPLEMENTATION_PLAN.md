## 阶段 1: 外挂歌词和专辑封面
**目标**: 根据已有音频元数据补齐同目录 `.lrc`、`.txt` 和 `cover.*` 文件。
**成功标准**: 只补缺失文件，不覆盖已有文件；接口未找到或失败时记录结果并继续处理。
**测试**: LRCLIB 响应、iTunes 搜索响应、文件写入和跳过已有文件测试。
**状态**: 已完成

## 阶段 2: Web 控制台
**目标**: 提供按钮触发去重、歌词、封面和全部操作，并显示任务状态、日志和报告摘要。
**成功标准**: HTTP 服务可在 Docker 中启动，任务不会并发执行，按钮操作真实调用现有逻辑。
**测试**: Web API 状态、启动任务、重复任务保护和静态页面检查。
**状态**: 已完成

## 阶段 3: Docker、Compose 和文档
**目标**: 让 Dockge 直接运行常驻 Web 服务，并保留一次性命令和群晖定时脚本。
**成功标准**: Compose 配置可解析，Web 端口、路径、令牌和数据报告说明完整。
**测试**: 单元测试、编译检查、Compose 配置解析、脚本语法检查和 GitHub Actions 镜像构建。
**状态**: 进行中
